# Copilot Instructions

## Project Guidelines
- User prefers C# using declarations (`using var ...`) instead of block-scoped using statements and does not want explicit `connection.Close()` calls when disposal handles cleanup.
- User prefers not using the `sealed` keyword on test classes unless required.